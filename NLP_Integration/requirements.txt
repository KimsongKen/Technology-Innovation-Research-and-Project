# Secure bridge FastAPI runtime (production).
catboost>=1.2.0
fastapi>=0.109.0
python-multipart>=0.0.9
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
numpy>=1.26.0
joblib>=1.3.0
sentence-transformers>=2.3.0

# Optional cloud ASR (SACA_USE_HOSTED_STT=1; OpenAI-compatible transcription API).
httpx>=0.27.0

# Optional speech-to-text backends (enabled via SACA_USE_FASTER_WHISPER / SACA_USE_WHISPER_TINY_FALLBACK).
faster-whisper>=1.0.0
openai-whisper>=20231117

# Optional Meta MMS ASR for Warlpiri voice intake (SACA_USE_MMS_WARLPIRI_STT=1). Large download.
torch>=2.0.0
transformers>=4.30.0
